apiVersion: apps/v1
kind: Deployment
metadata:
  name: d-java
 # namespace: development
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java
  template:
    metadata:
      labels:
        app: java
    spec:
      containers:
      - name: java
        image: nginx
        resources:
          requests:
            cpu: 200m
            memory: 400Mi
          limits:
            cpu: 300m
            memory: 800Mi
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: svc-java
  #namespace: development
 


spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: LoadBalancer
  selector:
    app: java